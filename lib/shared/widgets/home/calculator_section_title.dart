import 'package:flutter/material.dart';

class CalculatorSectionTitle extends StatelessWidget {
  const CalculatorSectionTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
