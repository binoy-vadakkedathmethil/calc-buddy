import 'package:flutter/material.dart';

class CalculatorSearch extends StatelessWidget {
  const CalculatorSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
